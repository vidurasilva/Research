{% extends 'AdminBundle:CRUD:default.html.twig' %}

{% block crud_default %}
    <table class="table table-bordered table-striped">
        <thead>
        <tr>
            <th>Id</th>
            <th>Icon</th>
            <th>Title</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for item in entity %}
            <tr>
                {#{% if item.parent %}#}
                  {##}
                {#{% endif %}#}
                <td><a href="{{ path('community_show', { 'id': item.id }) }}">{{ item.id }}</a></td>
                <td><img src="{{ asset('data/uploads') }}/{{ item.icon }}" width="25px"/> </td>
                <td><a href="{{ path('community_questions_overview', { 'id': item.id }) }}">{{ item.title }}</a></td>
                {#<td>{{ item.modified }}</td>#}
                <td>
                    <div class="vertical-top action-buttons">
                        <a href="{{ path('community_questions_overview', { 'id': item.id }) }}">
                            <button class="btn btn-green btn-icon btn-action">
                                <i class="fa fa-eye"></i>
                                <span>Show questions</span>
                            </button>
                        </a>
                        <a href="{{ path('community_edit', { 'id': item.id }) }}">
                            <button class="btn btn-green btn-icon btn-action">
                                <i class="fa fa-edit"></i>
                                <span>Edit</span>
                            </button>
                        </a>
                        <a href="{{ path('community_delete', { 'id': item.id }) }}">
                            <button class="btn btn-red btn-icon btn-action">
                                <i class="fa fa-remove"></i>
                                <span>Delete</span>
                            </button>
                        </a>
                    </div>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <div class="form-group">
        <a href="{{ path('community_create') }}"><button class="btn btn-white">Create new community</button></a>
    </div>
{% endblock %}
